import { ResaltarPokemon } from './resaltar-pokemon';

describe('ResaltarPokemon', () => {
  it('should create an instance', () => {
    const directive = new ResaltarPokemon();
    expect(directive).toBeTruthy();
  });
});
